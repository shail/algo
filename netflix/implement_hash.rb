class Hash
  def initialize()
    @array = Array.new(10) { [] }
  end

  def insert()

  end
end

# Basically it is an array of arrays, but instead of the array for collision detection we can use a linked
# list because it is easy to reason about
